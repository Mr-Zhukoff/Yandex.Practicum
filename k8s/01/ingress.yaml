---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bomberman
  annotations:
    cert-manager.io/cluster-issuer: "http01-clusterissuer"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
      - <название namespace из kubeconfig.yaml>-bomberman.k8sfree.praktikum-services.tech
    secretName: domain-name-secret
  rules:
  - host: <название namespace из kubeconfig.yaml>-bomberman.k8sfree.praktikum-services.tech
    http:
      paths:
      - backend:
          service:
            name: bomberman
            port:
              number: 8000
        path: /
        pathType: Prefix